{
  "name": "satori-popup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wp-scripts build && npm run build:css",
    "build:css": "tailwindcss -i ./src/popup.css -o ./build/style-index.css --minify",
    "start": "wp-scripts start"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.0.0",
    "tailwindcss": "^3.4.0"
  }
}
